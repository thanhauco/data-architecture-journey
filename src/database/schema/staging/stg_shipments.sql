-- Schema definition for shipments in staging layer
-- Generated by Data Architect

CREATE TABLE IF NOT EXISTS staging.stg_shipments (
    shipments_id BIGINT PRIMARY KEY,
    -- Core Fields
    name VARCHAR(255),
    description TEXT,
    status VARCHAR(50),
    type VARCHAR(50),
    
    -- Metrics
    amount DECIMAL(19, 4),
    quantity INT,
    score DOUBLE PRECISION,
    
    -- Metadata
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    source_system VARCHAR(100),
    ingestion_batch_id UUID,
    
    -- Staging Specific
    raw_payload JSONB
);

-- Indices
CREATE INDEX idx_stg_shipments_status ON staging.stg_shipments(status);
CREATE INDEX idx_stg_shipments_created ON staging.stg_shipments(created_at);
