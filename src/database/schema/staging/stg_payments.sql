-- Schema definition for payments in staging layer
-- Generated by Data Architect

CREATE TABLE IF NOT EXISTS staging.stg_payments (
    payments_id BIGINT PRIMARY KEY,
    -- Core Fields
    name VARCHAR(255),
    description TEXT,
    status VARCHAR(50),
    type VARCHAR(50),
    
    -- Metrics
    amount DECIMAL(19, 4),
    quantity INT,
    score DOUBLE PRECISION,
    
    -- Metadata
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    source_system VARCHAR(100),
    ingestion_batch_id UUID,
    
    -- Staging Specific
    raw_payload JSONB
);

-- Indices
CREATE INDEX idx_stg_payments_status ON staging.stg_payments(status);
CREATE INDEX idx_stg_payments_created ON staging.stg_payments(created_at);
